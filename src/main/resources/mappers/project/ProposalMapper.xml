<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.assistant.acc.utility.ProposalMapper">

    <!-- 파일 저장 경로 DB 기록 -->
    <insert id="insertProposalFile" parameterType="com.assistant.acc.utility.ProposalFileDTO">
        INSERT INTO proposal_file_path
        (user_input_no, proposal_file_path, create_at, proposal_file_name)
        VALUES
        (#{userInputNo}, #{proposalFilePath}, #{createAt}, #{proposalFileName})
    </insert>

</mapper>
